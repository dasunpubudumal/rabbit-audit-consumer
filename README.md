# Rabbit Audit Consumer

```
> ./rabbit-audit-consumer --help
Usage: rabbit-audit-consumer --host <HOST> --port <PORT> --vhost <VHOST> --username <USERNAME> --password <PASSWORD> --queue-name <QUEUE_NAME>

Options:
      --host <HOST>              
      --port <PORT>              
      --vhost <VHOST>            
      --username <USERNAME>      
      --password <PASSWORD>      
      --queue-name <QUEUE_NAME>  
  -h, --help                     Print help
  -V, --version                  Print version
```
